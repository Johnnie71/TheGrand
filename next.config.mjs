/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['aareas.com', 'images.unsplash.com'], // Allow images from any domain (use with caution)
  },
};

export default nextConfig;
